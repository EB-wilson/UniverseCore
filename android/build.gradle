plugins {
  id 'java'
  id 'com.github.johnrengelman.shadow'
}

publishing {
  publications {
    maven(MavenPublication) {
      groupId = 'com.github.EB-wilson'
      artifactId = 'android'
      version = "$uncVersion"
      from components.java
    }
  }
}

dependencies {
  repositories{
    mavenCentral()
    maven{ url 'https://www.jitpack.io' }
  }

  implementation project(":dynamilizer")

  compileOnly 'org.ow2.asm:asm:9.3'

  compileOnly "com.github.Anuken.Arc:arc-core:$mindustryVersion"
  compileOnly "com.github.Anuken.Mindustry:core:$mindustryVersion"
  implementation 'com.jakewharton.android.repackaged:dalvik-dx:9.0.0_r3'

  compileOnly project(":core")
  compileOnly project(":abstract")
  compileOnly project(":implabstract")
  compileOnly project(":utilities")

  annotationProcessor "com.github.Anuken:jabel:0.7.0"

  testRuntimeClasspath "com.jakewharton.android.repackaged:dalvik-dx:9.0.0_r3"
  testRuntimeClasspath project(":core")
  testRuntimeClasspath project(":abstract")
  testRuntimeClasspath project(":implabstract")
  testRuntimeClasspath project(":utilities")
}

shadowJar {
  dependencies {
    exclude dependency("com.github.Anuken.Arc:arc-core:$mindustryVersion")
    exclude dependency("com.github.Anuken.Mindustry:core:$mindustryVersion")
    exclude dependency("com.jakewharton.android.repackaged:dalvik-dx:9.0.0_r3")

    exclude project(":core")
    exclude project(":abstract")
    exclude project(":implabstract")
    exclude project(":utilities")
  }
}
